from typing import Any, Dict

def format_experiment_results(results: Dict[str, Any]) -> Dict[str, Any]:
    return results

def validate_experiment_config(config: Dict[str, Any]) -> bool:
    return True

